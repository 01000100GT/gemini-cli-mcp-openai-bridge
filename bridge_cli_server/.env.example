# ===========================================
# 单独的API Key配置（用于rotation-state.json引用）
# ===========================================
GEMINI_API_KEY=AI 
# 主账号ssj的API Key
GEMINI_API_KEY_SSJ=AIzaS 

# 备用账号mycola的API Key
GEMINI_API_KEY_MYCOLA=AI 

# 备用账号2iam01000100的API Key
GEMINI_API_KEY_IAM01000100=AI 


# ===========================================
# OpenAI兼容接口默认参数配置
# ===========================================

# 默认温度参数（控制输出的随机性，范围0-2）
DEFAULT_TEMPERATURE=0.7

# 默认最大令牌数（控制输出长度）
DEFAULT_MAX_TOKENS=65536

# 默认流式输出设置（true/false）
DEFAULT_STREAM=true

# 默认调试模式设置（true/false）
DEFAULT_DEBUG=false

# ===========================================
# Gemini CLI 执行超时配置（秒）
# ===========================================

# 默认超时时间（简单对话）
DEFAULT_TIMEOUT=180

# Function Calling 超时时间
FUNCTION_CALL_TIMEOUT=360

# 复杂查询超时时间（沙盒模式、文件处理等）
COMPLEX_QUERY_TIMEOUT=480

# 最大超时时间限制
MAX_TIMEOUT=600