# 多账号配置示例环境变量文件
# 复制此文件为 .env 并填入您的实际配置

# =============================================================================
# 多账号配置 (JSON格式)
# =============================================================================
# 将多账号配置设置为JSON字符串
# 注意：实际使用时需要将换行符和引号正确转义
GEMINI_MULTI_ACCOUNTS='[
  {
    "id": "mycola",
    "name": "主账号",
    "authType": "gemini-api-key",
    "apiKey": "AIzaSyAf",
    "status": "active",
    "proUsageCount": 0,
    "proQuotaLimit": 50
  },
  {
    "id": "songsj",
    "name": "备用账号",
    "authType": "gemini-api-key",
    "apiKey": "AIzaSy",
    "status": "active",
    "proUsageCount": 0,
    "proQuotaLimit": 50
  }
]'

# 或者使用简化的单行格式：
# GEMINI_MULTI_ACCOUNTS='[{"id":"account_1","name":"主账号","authType":"gemini-api-key","apiKey":"YOUR_API_KEY_1","status":"active","proUsageCount":0,"proQuotaLimit":50},{"id":"account_2","name":"备用账号","authType":"gemini-api-key","apiKey":"YOUR_API_KEY_2","status":"active","proUsageCount":0,"proQuotaLimit":50}]'

# =============================================================================
# 其他多账号相关配置
# =============================================================================

# Flash回退模型
GEMINI_FLASH_FALLBACK_MODEL=gemini-2.5-flash

# 账号轮换策略 (round_robin | least_used | random)
GEMINI_ROTATION_STRATEGY=round_robin

# 是否启用Flash回退 (true | false)
GEMINI_ENABLE_FLASH_FALLBACK=true

# =============================================================================
# 单个API Key配置 (用于单账号模式或作为备用)
# =============================================================================

# 主要的Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Google API Key (备用)
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# Vertex AI 配置
# =============================================================================

# Google Cloud项目ID
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud区域
# GOOGLE_CLOUD_LOCATION=us-central1

# 启用Vertex AI
# GOOGLE_GENAI_USE_VERTEXAI=true

# =============================================================================
# 服务器配置
# =============================================================================

# MCP服务器端口
GEMINI_MCP_PORT=8765

# 默认工具模型
GEMINI_TOOLS_DEFAULT_MODEL=gemini-2.5-pro

# 默认模型
GEMINI_MODEL=gemini-2.5-pro

# =============================================================================
# 使用说明
# =============================================================================
# 
# 1. 启用多账号模式：
#    npm start -- --enable-multi-account
# 
# 2. 指定轮换策略：
#    npm start -- --enable-multi-account --account-rotation-strategy=least_used
# 
# 3. 禁用Flash回退：
#    npm start -- --enable-multi-account --disable-flash-fallback
# 
# 4. 完整示例：
#    npm start -- --enable-multi-account --account-rotation-strategy=round_robin --debug
# 
# =============================================================================
# 配额管理说明
# =============================================================================
# 
# - 系统会自动跟踪每个账号的Pro模型使用次数
# - 当账号达到配额限制时，会自动切换到下一个可用账号
# - 当所有账号的Pro配额都用完时，会自动切换到Flash模型
# - 可以通过API或重启服务来重置配额计数
#